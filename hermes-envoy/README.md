# Hermes Envoy

Hermes-side Agent Envoy plugin design.

Status: spec-first. Build from `SPEC.md`.

MVP: a Hermes plugin that lets a Hermes agent use a short-lived Agent Envoy grant to consult a remote OpenClaw agent over the requester WebSocket protocol, with local redaction, prompt-size limits, audit logs, and session controls.
